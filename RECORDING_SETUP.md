# 녹화 전 세팅 — 이대로만 해두면 됨

## 핵심: 대본이 영상에 찍히면 안 된다

그래서 **화면 전체를 녹화하지 않는다.** 브라우저만 네모로 잡아서 녹화하고,
대본은 그 네모 **바깥**에 둔다.

```
┌─────────────────────────────────────┬──────────────┐
│                                     │              │
│   브라우저 (여기만 녹화됨)            │   대본       │
│   화면 왼쪽 3분의 2                  │   오른쪽     │
│                                     │   3분의 1    │
│                                     │              │
└─────────────────────────────────────┴──────────────┘
        ↑ Cmd+Shift+5 로 이 영역만 지정        ↑ 안 찍힘
```

---

## 1. 브라우저 탭 3개 (순서대로)

전부 같은 창에 탭으로 열어두면 `Cmd + 1 / 2 / 3` 으로 넘길 수 있다.

**탭 1** — 공개 화면 (대본 1·2·4·6번에서 씀)
```
https://shud26.github.io/copy-check/
```

**탭 2** — 세 가지 규칙 (대본 3번)
```
https://github.com/shud26/copy-check#what-this-tool-does-instead
```

**탭 3** — 서브그래프 코드 (대본 5번)
```
https://github.com/shud26/copy-check/blob/main/graph.py#L26-L55
```

세 개 다 브라우저라서 **앱을 왔다갔다 할 필요가 없다.** 탭만 바꾸면 된다.

---

## 2. 글자 크게

녹화하면 글자가 작아 보인다. 세 탭 모두에서 **`Cmd + +` 를 두 번씩** 눌러 확대해둔다.
(원래대로는 `Cmd + 0`)

---

## 3. 대본 띄우기

Cursor로 `VIDEO_SCRIPT.md` 열고 **`Cmd + Shift + V`** (미리보기).
창을 화면 오른쪽에 붙여둔다. 녹화 영역 밖이라 안 찍힌다.

---

## 4. 정리해둘 것

- 브라우저 **북마크바 숨기기** (`Cmd + Shift + B`) — 개인 북마크가 찍히면 곤란하다
- 다른 탭은 닫기. 탭 제목이 영상에 남는다
- 알림 끄기: 제어 센터 → **집중 모드 → 방해금지**
- 텔레그램·메일 알림이 뜨면 그 부분을 잘라내야 한다

---

## 5. 녹화 시작

1. `Cmd + Shift + 5`
2. 위쪽 아이콘 중 **오른쪽에서 두 번째** (Record Selected Portion) 선택
3. 브라우저 영역을 네모로 드래그
4. **`Options` → `Microphone` → 마이크 선택** ← 이거 안 하면 무음으로 찍힌다
5. `Record` 누르기
6. 끝나면 메뉴바 위쪽의 ⏹ 정지 버튼

---

## 6. 순서 요약 (녹화 중에 볼 것)

| 대본 | 탭 | 화면에서 할 일 |
|---|---|---|
| 1. 여는 말 | 1 | 페이지 맨 위 |
| 2. 문제 | 1 | 그대로 |
| 3. 세 규칙 | **2** | 규칙 1·2·3 천천히 스크롤 |
| 4. 실제 화면 | **1** | 데모 카드 → 실제 카드 3개 → alt-7a7a 가리키기 |
| 5. The Graph | **3** | 코드 보여주기 |
| 6. 닫는 말 | **1** | 맨 위로 |

탭 이동은 `Cmd + 1`, `Cmd + 2`, `Cmd + 3`.

---

## 7. 저장

바탕화면에 `.mov`로 저장된다. 그대로 제출하면 된다.

⚠️ **배속 편집 금지.** 잘라내기는 괜찮다. 2~4분 · 720p 이상 · 배경음악 없이.
