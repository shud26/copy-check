# 실측으로 알게 된 것 (2026-09-02~03)

대회 기간에 다시 밟지 않으려고 적어둔다.
전부 문서에 없거나, 문서와 다르게 동작한 것들이다.

## 1. urllib 기본 User-Agent 는 Cloudflare 가 막는다

```
Python-urllib/3.x  →  error code: 1010  (요청이 게이트웨이에 닿지도 못함)
브라우저 UA        →  통과
```

에러 메시지가 "키가 잘못됐다"처럼 보여서 API 키를 의심하며 한참 헤맸다.
실제로는 인증 이전 단계에서 잘리고 있었다.

## 2. 새 API 키는 게이트웨이 반영에 시간이 걸린다

Studio 에서 `ACTIVE` 로 보이고 Security 제한도 없는데
`auth error: API key not found` 가 계속 났다. 몇 분 뒤 그냥 통과했다.
→ 키를 만들자마자 실패해도 설정을 뒤지기 전에 몇 분 기다려볼 것.

## 3. 중첩 필터가 24배 느리고, 결국 실패한다

```
where: { transaction_: { blockNumber_gte: N } }   →  40초 후 bad indexers
where: { timestamp_gte: T }                      →  1.7초
```

블록 기준으로 자르고 싶어도 **시각으로 걸러서 받고, 블록 필터는 받은 뒤에** 걸어야 한다.

## 4. orderDirection 을 잘못 쓰면 조용히 망가진다

`asc` 로 받으면 스왑이 많은 지갑에서 페이지 상한에 걸려
**1만 건을 받고도 기준 블록 이후가 0건**이 나온다. 에러가 안 나서 더 위험하다.
→ 최신순(`desc`)으로 받고, 기준 블록에 닿으면 멈춘다.

## 5. transaction.gasUsed 는 0으로 채워져 있다

스키마에 필드는 있는데 값이 없다(유니스왑 v3 서브그래프).
→ tx 해시만 서브그래프에서 뽑고, 가스는 RPC 리시트로 따로 받는다.

**가스를 추정하면 안 되는 이유**: 실측 분포가 0.000030 ~ 0.003157 ETH 로 **104배** 차이였다.
그리고 어떤 지갑은 가스 전 `-0.00001`(사실상 본전)인데 가스 후 `-0.00197` 이었다.
**손실의 99%가 가스였다.**

## 6. ⚠️ 기축을 여러 개 두면 단위가 섞인다 (제일 큰 실수)

WETH·USDC·USDT·DAI 를 전부 기축으로 뒀더니 같은 WBTC 의 단가가
**32(ETH 기준)와 77,225(달러 기준)로 섞여** 들어왔고,
합산 결과가 **+299,923 ETH(1조원대)** 라는 허수가 나왔다.

기축을 WETH 하나로 고정하니 같은 지갑이 **-98 ETH** 로 뒤집혔다.

> 단위가 다르면 합산 금지. oc8에서 체인 간에 배운 것을 기축 간에 반복했다.

## 7. "자주 등장하는 지갑"으로 뽑으면 전부 봇이다

최근 스왑 5,000건에서 빈도순으로 상위 6개를 뽑았더니
**6개 전부 봇이거나 완결 0건**이었다. 자주 등장한다는 게 곧 봇의 정의다.

→ 카피 대상 선별은 별도의 문제다. 이 도구의 첫 관문이기도 하다.

## 8. 속도

지갑당 270~450초. 대부분 가스 조회다.
→ 병렬화하거나, 봇으로 판정되면 가스를 아예 받지 않는 식으로 줄여야 한다.
